pipeline:
  - stage(test):
    - tasks(parallel):
      - env:
          message: parallel one
      - shell:
          script: |
            sleep 2s
            echo "first - $message!"
      - shell:
          script: echo "second - $message!"